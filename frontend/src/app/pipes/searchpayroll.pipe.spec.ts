import { SearchpayrollPipe } from './searchpayroll.pipe';

describe('SearchpayrollPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchpayrollPipe();
    expect(pipe).toBeTruthy();
  });
});
