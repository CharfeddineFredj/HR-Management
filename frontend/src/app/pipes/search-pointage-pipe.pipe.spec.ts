import { SearchPointagePipePipe } from './search-pointage-pipe.pipe';

describe('SearchPointagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPointagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
