import { SearchReviewVactionPipe } from './search-review-vaction.pipe';

describe('SearchReviewVactionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReviewVactionPipe();
    expect(pipe).toBeTruthy();
  });
});
