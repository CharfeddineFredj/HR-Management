import { SearchschedulePipe } from './searchschedule.pipe';

describe('SearchschedulePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchschedulePipe();
    expect(pipe).toBeTruthy();
  });
});
