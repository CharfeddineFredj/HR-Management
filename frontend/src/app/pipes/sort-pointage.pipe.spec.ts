import { SortPointagePipe } from './sort-pointage.pipe';

describe('SortPointagePipe', () => {
  it('create an instance', () => {
    const pipe = new SortPointagePipe();
    expect(pipe).toBeTruthy();
  });
});
