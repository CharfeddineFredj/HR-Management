import { SearchHolidayPipe } from './search-holiday.pipe';

describe('SearchHolidayPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHolidayPipe();
    expect(pipe).toBeTruthy();
  });
});
