import { RechercheEmployeePipe } from './recherche-employee.pipe';

describe('RechercheEmployeePipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheEmployeePipe();
    expect(pipe).toBeTruthy();
  });
});
